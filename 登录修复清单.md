# 系统登录 & 权限修复清单

> ⚠️ **重要：后续任何 bug 修复和功能优化，必须对照此清单检查，不可破坏以下已修复的功能！**
> 每次修改登录相关代码、路由、数据库 migration 前，必须逐条验证。

## 已修复的 Bug 列表

### 1. 运营商超管登录 (13944444444)
- **密码：** `yVYXh1g4G8`
- **状态：** ✅ `password_change_required=0`（登录不弹改密窗）
- **权限：** `permissions: ['*']`（后端 auth.ts 硬编码返回，不依赖 admin_roles 表）
- **不受影响：** 运营商角色成员管理页面不显示超管，也不能创建超管

### 2. 总部超管 admin 登录
- **密码：** 用户已自行修改（初始密码 `admin8New!`）
- **状态：** ✅ `first_login=0`（弹窗改密成功后正常跳转运营后台）
- **登录方式：** 用用户名（`admin`）或手机号均可（后端 `phone` 兜底查询）

### 3. 角色成员管理 - 过滤超级管理员
- **运营商端：** `OperatorUserManage.tsx` 的 `ROLE_OPTIONS` 已去掉 `op_super_admin`
- **运营商后端：** `operator.ts` 的两个 SQL 查询都加了 `AND id != 'op_super_admin'`
- **总部后端：** `admin-rbac.ts` 的 SQL 查询加了 `AND id != 'role-super-admin'`

### 4. 首次登录改密弹窗统一
- **三个入口都统一：** 总部后台、运营商后台、裁判端
- **字段：** 当前密码 + 新密码（≥8位+大小写+数字） + 确认新密码
- **使用 Modal**，不能用 close 或 mask 关闭

### 5. 运营商登录字段
- **已支持手机号登录**（`operator-member-login` 接口用 `phone` 字段）
- **总部后台登录**输入框已改为"手机号/用户名"

### 6. 数据库 `client_logs` 表
- 用于前端 XHR/fetch/onerror/console.error 劫持上报
- 头部连接数据库后确认表存在

## 已删除的数据
- `admin_users` 表中 `13955555555` 的记录（可重新创建）
- `admin_users` 表中 `13944444444` 的旧角色成员记录（保证不污染运营商超管）

## 不再尝试的方案
- Chrome 远程调试端口 attach（所有方案均验证失败）
- 客户端日志系统无法捕获页面加载失败场景的报错
