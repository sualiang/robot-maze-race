# 故障台账

## 2026-06-27: session file locked + CLI 命令歧义故障

### 故障现象
- 会话锁文件残留导致 "session file locked" 报错
- CLI 命令 `openclaw agent` 存在歧义（`agents` vs `agent`），导致运维误操作
- 僵死进程 73800 虽已消失，但历史报错需重启才能消除

### 根因
1. 进程异常退出时未自动清理会话锁文件
2. CLI 命令名称不统一（`agents` / `agent`），冗余参数校验拦截了正常指令
3. 无自动巡检发现超时死锁

### 岗位分派

| 岗位 | 任务 | 优先级 |
|------|------|--------|
| 03-后端开发 | ①补齐 CLI 单 Agent 启停命令 `openclaw agents start/stop/restart <agent名>`；②优化会话锁逻辑（退出自动清理 + 启动自检删残留）；③移除 agent 发消息指令外冗余参数校验 | P1 |
| 07-DevOps | ①更新 ops-bot 巡检脚本（扫描会话目录、识别死锁、自动 kill+删锁）；②整理 MacOS launchd 强制重启流程 → 运维手册；③标准化故障处理命令，更新 MCP 故障台账，同步本次流程给 01 归档 | P0 |

### 07-DevOps 完成记录（2026-06-27 16:01）

| 交付物 | 文件路径 | 状态 |
|--------|----------|------|
| ① ops-bot 巡检脚本（section 8 会话锁死锁巡检） | `scripts/ops-bot.sh` §8 | ✅ 完成 |
| ② MacOS launchd 强制重启流程（运维手册） | `mcp/01-project/macos-launchd-restart-guide.md` | ✅ 完成 |
| ③ 标准故障处理命令集 | `mcp/01-project/session-lock-recovery-commands.md` | ✅ 完成 |
| ④ MCP 故障台账已更新 | `mcp/01-project/fault-log.md`（本文件） | ✅ 完成 |

#### ① ops-bot 新增功能
- 扫描 `~/.openclaw/agents/*/sessions/` 下所有 `.jsonl.lock` 文件
- 解析锁文件 JSON（pid / createdAt / maxHoldMs）
- 判断标准：PID 已死 + 创建时间 > 30分钟（或 > maxHoldMs/1000+5min 取大值）→ 死锁
- 自动 kill -9 残留进程 + 删除锁文件
- 告警输出到报告文件并计入退出码

#### ② 运维手册要点
- 诊断命令（进程检查、锁文件检查、日志检查）
- 三种恢复场景（进程僵死 / 进程已消失锁残留 / 完全重启含看门狗）
- 一键恢复三合一行命令
- 验证检查清单（8项）
- launchd 命令速查
- 特别注意：**删锁 → 重启** 缺一不可

#### ③ 命令集覆盖
- 7 条标准命令编号（①~⑦），按严重程度分级
- 诊断 → 恢复 → 验证 三段式组织
- 包含本次故障速查表

### 我的跟进（02-小D）
- [x] 验收 07 的运维手册和巡检脚本更新（2026-06-27 16:01 完成）
- [x] 确认重启后 session file locked 不再复现（2026-06-27 16:01 确认）
- [x] 同步本次故障复盘到 MCP 知识库（故障台账 + 运维手册 + 命令集已归档）
- [ ] 验收 03 的代码修复

### 状态
- [x] 03-后端开发 — 未启动（等待 03 回合）
- [x] 07-DevOps — 已完成（2026-06-27 16:01）

----

## 2026-06-27 前：历史故障占位
*（可在后续补充其他故障记录）*
