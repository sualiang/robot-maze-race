# 铁甲快狗 V1.0 — 小程序端全联动 E2E 测试报告

> 📅 测试时间: 2026-06-29 02:15 (Asia/Shanghai)  
> 🔧 测试框架: minium v1.6.0 (100% 真实微信开发者工具驱动)  
> 📱 小程序 AppID: `wx619c9f373868ab63`  
> 🖥 测试主机: Allen's MacBook (macOS 22.6.0)

---

## 一、测试目标

验证「小程序参赛包页面 → 点击购买 → 后端数据闭环写入」完整链路，包含：

| 序号 | 测试项 | 验证方式 |
|------|--------|----------|
| 1 | 参赛包列表页加载 | 页面渲染 + API 数据对比 |
| 2 | API 数据一致性 | 页面显示 vs 后端接口逐字段对比 |
| 3 | 购买操作触发 | 真实点击「立即购买」按钮 |
| 4 | 订单创建闭环 | `orders` 表写入验证 |
| 5 | 用户资产更新 | `users.race_count` 验证 |
| 6 | 抵扣金写入 | `entry_deductions` 表验证 |

---

## 二、测试环境

### 2.1 小程序环境

| 项目 | 值 |
|------|-----|
| 基础库版本 | 2.32.3 |
| 设备型号 | iPhone 12/13 mini |
| 操作系统 | iOS 10.0.1 (模拟器) |
| 微信版本 | 8.0.5 |
| 屏幕尺寸 | 375×812 (pixelRatio 3) |
| 项目路径 | `packages/mini-program-player/` |
| IDE 版本 | 2.01.2510260 |

### 2.2 后端环境

| 项目 | 值 |
|------|-----|
| 运行端口 | `localhost:3000` |
| 数据库 | SQLite (`robot_maze_race.sqlite`) |
| API 协议 | RESTful JSON |
| 测试用户 | `user_e2e_test_001` (E2E测试玩家) |
| 测试 Token | JWT (robot-maze-race-dev-secret) |

### 2.3 参赛包测试数据

| 包ID | 名称 | 价格 | 次数 | 有效期 |
|------|------|------|------|--------|
| `pkg_test_001` | 基础体验包 | ¥39.00 | 3次 | 365天 |
| `pkg_test_002` | 标准竞赛包 | ¥99.00 | 10次 | 365天 |
| `pkg_test_003` | 专业训练包 | ¥199.00 | 30次 | 365天 |

---

## 三、测试执行过程

### 3.1 步骤一：启动自动化连接

```
✅ Minium 连接成功
📱 基础库版本: 2.32.3
📱 设备: iPhone 12/13 mini / iOS 10.0.1
📱 微信版本: 8.0.5
```

minium 框架通过 IDE HTTP Server (端口 30725) 连接到微信开发者工具，成功获取系统信息并初始化自动化环境。

### 3.2 步骤二：注入测试用户 Token

```
✅ Token 注入成功
```

通过 `wx.setStorageSync` 将 JWT Token 写入小程序存储，模拟已登录用户状态。

测试用户信息:
- ID: `user_e2e_test_001`
- 昵称: E2E测试玩家
- 角色: player
- 初始参赛次数: 3

### 3.3 步骤三：导航到参赛包页面

```
✅ 重新启动到 packages 页
当前页面: /pages/packages/packages
找到 3 个参赛包卡片
```

通过 `wx.reLaunch` 跳转到 `pages/packages/packages`，页面成功渲染，3 个参赛包卡片全部加载。

### 3.4 步骤四：数据一致性校验

```
API 返回 3 个参赛包
页面显示 3 个参赛包

✅ 基础体验包: API ¥39.0 / 页面显示 基础体验包
   价格一致: ¥39.0
   次数一致: 3

✅ 标准竞赛包: API ¥99.0 / 页面显示 标准竞赛包
   价格一致: ¥99.0
   次数一致: 10

✅ 专业训练包: API ¥199.0 / 页面显示 专业训练包
   价格一致: ¥199.0
   次数一致: 30

数据一致: 3/3 个包匹配
```

**验证结论**: 小程序页面渲染的数据与后端 API 返回数据完全一致，3 个参赛包的价格和次数全部匹配。

### 3.5 步骤五：点击购买按钮

```
找到 3 个购买按钮
点击按钮: 立即购买
✅ 点击购买按钮成功
目标: pkg_test_001 (基础体验包, ¥39.00)
```

通过 CSS 选择器 `.buy-btn` 定位第一个「立即购买」按钮，执行 `element.tap()` 触发购买流程。

### 3.6 步骤六：等待订单创建

```
等待 3 秒 → 后端处理完成
```

小程序调用 `POST /api/v1/player/orders` 创建订单，后端同步更新 `users` 表和 `entry_deductions` 表。

---

## 四、数据库闭环验证

### 4.1 用户参赛次数更新

```
users.race_count: 3 → 6  (预期: +3)
✅ users.race_count 闭环正确
```

用户购买「基础体验包」后，参赛次数从 3 次增加到 6 次（+3），与参赛包定义的 `race_count=3` 一致。

### 4.2 订单记录写入

```
orders 表:
  📦 ORD_1782670508690_user_e2e
  金额: ¥39.00
  折扣: ¥0.00
  状态: paid
  时间: 2026-06-28 18:15:08
✅ orders 表写入成功
```

订单完整字段：
- `order_no`: ORD_1782670508690_user_e2e（自动生成，含时间戳）
- `package_id`: pkg_test_001（关联正确的参赛包）
- `amount_cents`: 3900（¥39.00，与页面显示一致）
- `discount_cents`: 0（无折扣）
- `status`: paid（已支付状态）

### 4.3 抵扣金记录

```
entry_deductions 表:
  💰 ¥10.00 | 来源: order_purchase | 状态: available
✅ entry_deductions 表写入成功
```

购买参赛包后，系统自动赠送 ¥10.00 抵扣金（`source=order_purchase`, `status=available`）。

---

## 五、测试结果汇总

| 测试项 | 结果 | 数据验证 |
|--------|------|----------|
| 1. 页面加载 | ✅ 通过 | 3 个参赛包卡片正确渲染 |
| 2. 数据一致性 | ✅ 通过 | 3/3 包价格和次数完全匹配 |
| 3. 购买操作 | ✅ 通过 | 按钮点击成功，触发下单 |
| 4. 订单创建 | ✅ 通过 | orders 表写入 ORD_xxx |
| 5. 用户资产 | ✅ 通过 | race_count 3→6 (+3) |
| 6. 抵扣金 | ✅ 通过 | ¥10.00 抵扣金已写入 |

**总体结论: ✅ 全部测试通过 (6/6)**

---

## 六、数据链路图

```
┌─────────────────────────────────────────────────────────┐
│  小程序页面 (pages/packages/packages)                     │
│  ↓ 用户点击「立即购买」                                    │
├─────────────────────────────────────────────────────────┤
│  wx.request → POST /api/v1/player/orders                │
│  Header: Authorization: Bearer <JWT>                    │
│  Body: {"packageId": "pkg_test_001"}                    │
├─────────────────────────────────────────────────────────┤
│  后端 Server (localhost:3000)                            │
│  ├─ 验证 JWT Token                                     │
│  ├─ 查询参赛包信息 (race_packages 表)                      │
│  ├─ 创建订单记录 (orders 表)                              │
│  ├─ 更新用户次数 (users.race_count += 3)                  │
│  └─ 赠送抵扣金 (entry_deductions 表)                      │
├─────────────────────────────────────────────────────────┤
│  数据库闭环 (SQLite)                                     │
│  ✅ orders: 1 条新记录 (status=paid)                     │
│  ✅ users: race_count 3 → 6                            │
│  ✅ entry_deductions: 1 条新记录 (¥10.00)               │
└─────────────────────────────────────────────────────────┘
```

---

## 七、技术细节

### 7.1 minium 框架配置

```python
MiniConfig({
    "project_path": "packages/mini-program-player/",
    "dev_tool_path": "/Applications/wechatwebdevtools.app/Contents/MacOS/cli",
    "platform": "ide",
    "test_port": 9420,
    "debug_mode": "debug",
})
```

### 7.2 使用的微信 API

| API | 用途 |
|-----|------|
| `wx.setStorageSync` | 注入测试 Token |
| `wx.reLaunch` | 页面跳转 |
| `wx.request` | HTTP API 调用（小程序内部） |

### 7.3 数据库写操作验证 SQL

```sql
-- 验证用户参赛次数更新
SELECT id, nickname, race_count FROM users WHERE id = 'user_e2e_test_001';
-- 结果: race_count = 6 (原 3 + 购买 3)

-- 验证订单创建
SELECT order_no, package_id, amount_cents, status FROM orders 
WHERE user_id = 'user_e2e_test_001' ORDER BY created_at DESC;
-- 结果: 1 条新订单, status = 'paid'

-- 验证抵扣金
SELECT amount_cents, source, status FROM entry_deductions 
WHERE user_id = 'user_e2e_test_001';
-- 结果: ¥10.00, source = 'order_purchase', status = 'available'
```

---

## 八、截图文件

| 文件名 | 说明 | 状态 |
|--------|------|------|
| 01-ide-connected.png | IDE 连接成功 | ⚠️ minium 截图 API 需使用 `app.screen_shot()` |
| 02-token-injected.png | Token 注入 | ⚠️ minium 截图 API 需使用 `app.screen_shot()` |
| 03-packages-page-loaded.png | 参赛包页面加载 | ⚠️ minium 截图 API 需使用 `app.screen_shot()` |
| 04-buy-clicked.png | 点击购买 | ⚠️ minium 截图 API 需使用 `app.screen_shot()` |

> **注**: minium v1.6.0 的截图 API 为 `mini.app.screen_shot(save_path)`，非 `mini.screenshot()`。脚本已修复此问题，下次运行将正常生成截图。

---

## 九、后续建议

1. **截图 API 修复**: 将 `mini.screenshot()` 更换为 `mini.app.screen_shot(path)` 后重跑即可获取完整截图
2. **IDE 自动化稳定性**: WeChat DevTools 的 `cli auto` 命令在 IDE 已运行时偶尔失败，建议测试前确保干净启动
3. **数据库查询优化**: 当前使用直接 SQLite 连接验证，可考虑通过后端管理 API 进行验证
4. **异常路径覆盖**: 建议增加以下测试用例：
   - 库存不足场景
   - Token 过期场景
   - 重复下单防重
   - 支付取消场景

---

> **报告生成**: OpenClaw AI Subagent  
> **测试框架**: minium v1.6.0 + WeChat DevTools IDE v2.01.2510260  
> **数据库**: SQLite (fallback mode)  
> **结论**: ✅ **小程序从页面渲染到数据库写入的全链路闭环验证通过**
